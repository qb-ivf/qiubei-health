# S0 阶段操作手册：天津监管平台对接前置准备

> 隶属于 [tianjin_supervision_plan.md](tianjin_supervision_plan.md) 的 S0 阶段展开。
> 背景：我院 **2023 年已取得互联网医院牌照**，此前由外包商（多租户 HIS SaaS，监管 SDK 供应商为纳里健康/ngari）代运营并可能一直以我院名义向监管平台上报。本次自研替换，S0 的核心不是"新申请入驻"，而是**收回账号控制权 → 摸清存量上报现状 → 拿到测试密钥 → 打通第一个请求**。
>
> ✅ 已提前完成的部分：官方 SDK jar 已到手并完成逆向对拍，**SM4/SM3/签名规则全部确认**（见 [tianjin_gateway_protocol.md](tianjin_gateway_protocol.md)，含黄金测试向量）；ICD-10 编码表、填报说明等资料已归档至 [docs/specs/tianjin/](specs/tianjin/)。原计划里"拿 SDK 对拍"这一最大不确定项已消除。
>
> 📌 **2026-07-03 进展**：已登录平台拿到"机构业务详情"（截图归档）——**测试环境密钥到手**（已配置进 `backend/.env`，不入 git），机构审核状态"通过"、接入报告"已上传"。T1 基本完成、T3 密钥部分完成、T7 organID 已确认。本地国密自检通过，**T4 只差在可出网的机器上执行一条命令**。
>
> 📌 **2026-07-07 进展（T5 平台书面答复到手）**：13 问全部获平台对接人书面回签（归档
> [T5-口径确认函](specs/tianjin/T5-口径确认函-待发平台.md) 答复登记表）。**无一项推翻实现**，触发：①代码改动
> 1 处——平台确认「无 uploadFile 接口」，采集器附件路径已停用（firstDiagnosis 外发空串，冒烟已证接受）；
> ②达标**可沿用**（不必刷量）；③正式流程明确=**上传互联网营业执照→审批部门发正式密钥**（测试/正式两独立通道）；
> ④预审核恢复有解=**运维大群 @毕老师**；⑤远程会诊去留待决策（平台不为"应报未报"兜底，建议取消勾选）。
>
> 📌 **2026-07-22 进展（正式密钥到手）**：机构业务详情已显示“执照已上传 / 材料审批通过 / 接入报告已上传”，
> 正式 appKey/appSecret 已发放。正式凭据已写入本地受 `.gitignore` 保护的 `backend/.env`，
> `TJ_REPORT_ENABLED=false`，**尚未向正式平台发送数据**。代码新增生产只读预检、首次真实药品目录入队、
> 正式网关防合成测试数据、防关闭期间吞队列，以及药师身份证补录入口。当前唯一业务阻断项仍是 T6 实际补录。

### 2026-07-22 本轮已完成事项

- [x] 平台“预审核”状态已恢复，机构详情显示“执照已上传 / 材料审批通过”；
- [x] 接入报告状态已确认“已上传”；
- [x] 正式网关地址、appKey、appSecret 已取得；
- [x] 正式凭据已写入本地 `backend/.env`，并确认该文件受 `.gitignore` 保护；
- [x] `TJ_REPORT_ENABLED=false`，本轮未向正式监管平台发送数据；
- [x] 新增正式切换只读预检 `scripts/tj_preflight.py`；
- [x] 新增首次真实药品目录入队工具 `scripts/tj_bootstrap_drugs.py`；
- [x] 正式环境关闭上报时改为保留 pending，避免任务被标成“模拟成功”而漏报；
- [x] 药品目录调整为优先于处方等业务任务发送；
- [x] `tj_ping.py`、`tj_smoke.py` 已增加正式网关防误写保护；
- [x] admin“账号管理”已增加药师身份证加密录入及“监管备案”状态；
- [x] 未完成监管备案的审方账号已禁止审方通过；
- [x] 后端测试 **124 passed**，admin-web 生产构建通过；
- [x] 正式凭据扫描确认未进入受 Git 管理的文件。

### 平台档案关键信息（非敏感部分，2026-07-03 抄录）

| 项 | 值 |
| :-- | :-- |
| 医疗机构名称 | 天津逑贝互联网医院 |
| 组织机构代码（organID） | `91120116MACJA9PX45` |
| 监管平台 unitID | `20250813151647906` |
| 测试网关（互联网） | `https://imssp.wsjk.tj.gov.cn/net-diag-service/test-openapi/api` |
| 测试网关（卫生专网备选） | `http://10.152.101.6:8080/net-diag-service/test-openapi/api` |
| 正式网关（互联网） | `https://imssp.wsjk.tj.gov.cn/net-diag-service/openapi/api` |
| 测试 appKey/appSecret | ✅ 已到手，**仅存 `backend/.env`**（appSecret 为 32 位 hex，格式符合预期） |
| 正式 appKey/appSecret | ✅ **2026-07-22 已发放**；仅存受忽略的环境配置，不写本文、不入 git |
| 申请类型/方式 | 互联网医院 / **医疗机构与第三方合作申请**（2025-08-21 提交）→ 佐证外包商参与，T2 摸底必做 |
| 审核状态 | ✅ 执照已上传，材料审批通过；接入报告已上传（2026-07-22 截图复核） |

---

## 任务一览（按顺序执行，标注责任角色与预估耗时）

| # | 任务 | 责任 | 耗时 | 状态 |
| :-: | :-- | :-- | :-: | :-- |
| T1 | 监管平台账号收权 | 负责人/法务 | 1–2 天 | 🟡 已可登录并见密钥页；余：改密+确认管理员归属 |
| T2 | 摸底外包商存量上报现状 | 负责人 + 研发C | 1 天 | ✅ **关闭**：外包商仅 2025-08 测试环境刷数达标，正式环境零上报；必接=10 接口（9 已实现+远程会诊待澄清） |
| T3 | 测试/正式密钥 + 权限/白名单 | 研发C | 0.5 天 | ✅ **关闭**：两套密钥均已取得；实测无 40006/40007，权限与白名单无障碍 |
| T4 | 首次连通性验证（tj_ping） | 研发B | 0.5 天 | ✅ **完成**（2026-07-04，生产容器内）：`msgCode=200 请求成功`，协议全链路兼容 |
| T5 | 口径确认清单发平台并取得回复 | 研发C | 3–5 天（等回复） | ✅ **关闭**（2026-07-07）：13 问全部书面回签，无一推翻实现；答复登记表已归档 [T5-口径确认函](specs/tianjin/T5-口径确认函-待发平台.md) |
| T6 | 字典对照、人员及药品资料补录 | 医务/药房 + 研发C | 3–5 天（与 T5 并行） | 🟡 **材料和录入功能全备**；余：医务/药房填表，运营实际录入并通过生产预检 |
| T7 | 机构信息与上线计划 | 负责人 | 0.5 天 | ✅ **完成**：organID、正式凭据、无并行期上线步骤及回退边界均已明确 |

全部完成即 S0 验收通过。S1–S4 研发已全部完成，**T4 已打通（2026-07-04），S5 联调即刻可开**。

### S5 启动指引（T4 打通后的第一步）

✅ **冒烟已通过（2026-07-04）**：[tj_smoke.py](../backend/scripts/tj_smoke.py)（payload 走生产映射层构建）
对 9 个必接接口各推 1 条测试数据，**9/9 全部 `msgCode=200`，零字段错误**（批次 QBTEST0704034042）。
我方上报代码与平台字段校验完全兼容，S5 联调核心完成。

剩余 S5 动作（T5 答复后收窄）：

1. ✅ 反显核对通过（2026-07-04 截图归档）：9 个接口计数精确 +1（药品目录 +2 含 tj_ping），
   平台逐条收到并计入，所有必接接口达标状态保持"是"。
2. ✅ **达标沿用已确认**（T5 Q3，2026-07-07）：监管接口/unitID 未变，历史达标直接用，
   **无需 `tj_smoke --count 50` 刷量**——此步取消。
3. ✅ **正式环境密钥已发放（2026-07-22）**；本地工作区已安全暂存正式网关与凭据，开关保持关闭，
   没有向正式平台写数据。生产服务器尚需在 T6 通过后按下方命令预检和切换。
4. ⚠️ 开正式前必须完成 **T6 补录**（医生科目/身份证、药师姓名/身份证、药品分类码）——真实数据缺这些字段会批量进死信。

---

## 🎯 上线临门操作清单（2026-07-22，按顺序照做）

> 平台答复到手后，上线只剩人工动作。以下三步**按序执行**，每步给出平台菜单入口与判定标准，
> 做完打勾即可推进到"开正式上报"。所需保护脚本与补录入口均已就绪。

### 第 1 步 · 恢复"预审核"状态 —— ✅ 结果已达成

误触材料变更导致"审核状态"从"执照已上传"变为**预审核**，必须先恢复，否则无法申请正式密钥。

- [x] 1.1 平台状态已恢复，2026-07-22 机构详情显示“执照已上传”；
- [x] 1.2 机构"天津逑贝互联网医院"（unitID `20250813151647906`）材料审批结果为“通过”；
- [x] 1.3 接入报告状态为“已上传”，正式密钥已可见；截图已作为本轮依据。

> 说明：结果状态已经复核通过，无需再追溯具体恢复方式。

### 第 2 步 · 决定远程会诊去留（建议：取消勾选）

平台未为"生产环境长期无数据是否应报未报"兜底 → **建议取消勾选**彻底消歧义（我院零业务、取消无损失）。
若决定维持保留，跳过本步、接受残余歧义即可（研发侧同样无需开发）。

**若取消勾选**（推荐）：

- [x] 2.1 确认第 1 步已恢复为"通过/执照已上传"；
- [ ] 2.2 进 `申请子系统 》 材料申报 》 申报详情 》 材料变更`；
- [ ] 2.3 走到**第 8 步"互联网名称信息"**的"互联网诊疗业务"勾选 → **仅取消"远程会诊"，其余勾选一律不动**；
- [ ] 2.4 走完 9~14 步提交（**全量重审，只核对不改其他内容**）；
- [ ] 2.5 等卫健委重新审核通过；
- [ ] 2.6 到「测试接口双向反显」确认远程会诊"是否需要对接"已变为**否**（截图归档）。

> ⚠️ 提交后会再次进入审核流程；若中途状态又卡在"预审核"，回第 1 步找毕老师。
> 取消成功后远程会诊彻底移出必接范围，正式环境无任何相关合规义务。

### 第 3 步 · 申请正式密钥并切生产

- [x] 3.1 互联网营业执照前置材料已获审批；
- [x] 3.2 机构详情显示“执照已上传 / 材料审批通过 / 接入报告已上传”；
- [x] 3.3 **正式网关地址 + 正式 appKey/appSecret 已发放**
      （测试/正式为两个独立通道，测试密钥不能用于正式）；
- [ ] 3.4 确认 **T6 补录已完成**（6 医师科目/身份证、4 药师身份证、药品分类码全部录入）——否则真实数据批量进死信；
- [ ] 3.5 在**生产容器**运行 `python scripts/tj_preflight.py`，必须为 **0 FAIL**；
- [ ] 3.6 保持开关关闭，预览并执行首次真实药品目录入队：
      `python scripts/tj_bootstrap_drugs.py`，确认后加 `--apply --confirm-unit 20250813151647906`；
- [ ] 3.7 服务器 `backend/.env` 确认正式凭据后设 `TJ_REPORT_ENABLED=true`，再 `dc restart api`；
- [ ] 3.8 先在 admin-web 核验药品目录任务成功；**次日早上**再核验 01:30 自动批次全部 `success`、无死信。

> 完成第 3 步即正式上线，进入 T+1 每日自动上报稳态。

---

## T1 · 监管平台账号收权（第一优先级）

机构填报端入口：**https://imssp.wsjk.tj.gov.cn/hosmit-hlwqd/home.html**（出自《互联网监管平台填报说明.doc》，已归档）。

1. 先在我方内部排查是否有账号记录：牌照申办（2023 年）时的经办人邮箱/手机号、与外包商合同附件、历史邮件里搜"监管平台/imssp/账号"。
2. 分两种情况：
   - **账号在我方手里**：登录验证，立即改密码，绑定我方负责人手机号。
   - **账号在外包商手里**：书面（邮件/函件）要求移交机构账号及"秘钥生成及管理"页面的全部凭据；同时向天津市卫健委信息中心/监管平台运维方报备机构管理员变更（凭牌照与执照可以走找回流程，不依赖外包商配合）。
3. 收权后记录：登录账号、管理员手机号、密码保管人。**先不要动"正式环境密钥重置"**——见 T7 切换窗口，贸然重置会立刻切断外包商可能仍在进行的生产上报，造成断报。
4. 顺带核查平台内已维护的机构信息：机构名称、组织机构代码、诊疗科目备案、医生备案名单是否与现状一致（这些是业务上报被校验的基础数据）。

**产出：** 我方可登录的账号 + 平台内机构档案截图存档。

## T2 · 摸底外包商存量上报现状 —— ✅ 结论完整（2026-07-03，两页截图交叉验证）

两页数据交叉：

- **「接口数据量统计」**（2025-01 ~ 2026-06）：全部接口 **0** → 该页统计的是**正式环境**；
- **「测试接口双向反显」**：9 个接口"完成对接=是、数据量达标=是"，已上传量 100/100/100/50/100/29/100/20/100
  （整数批量，典型联调造数痕迹）→ **外包商 2025-08 前后在测试环境完成过联调达标，但从未上线正式环境**
  （与正式密钥未发放互相印证）。

> **修正后结论**：正式环境零上报、无存量业务数据、无 bussID 冲突、无断报切换问题——
> **正式环境仍是从零接入**；但平台侧的"测试达标"状态可能已被外包商刷满，
> 我方或可**直接申请正式密钥**（能否沿用历史达标 → T5 新增问题）。

**平台标记的"需要对接"接口清单（重要，直接取代我方此前的推测矩阵）**：

| 平台标记需要对接 | 达标量 | 已传 | 我方现状 |
| :-- | :-: | :-: | :-- |
| 在线咨询 / 在线复诊 / 电子病历 / 在线处方 / 处方核销 | 50 | 100/100/100/100/100 | ✅ 已实现 |
| 医院药品目录备案 | 10 | 29 | ✅ 已实现 |
| 在线评价 | 10 | 20 | ✅ 已实现 |
| 医疗争议（不良事件） | 10 | 20 | ✅ 已实现（含每日签到，2026-07-04 补图确认必接+历史达标） |
| **预约挂号** | 50 | 100 | ✅ **确认必接，已补实现**（build_appoint + 每日批次） |
| **远程会诊** | 50 | 50 | ❗ 我院无此业务，疑为外包商当年勾选——T5 第 13 问；不能调则需开发 |
| 护理类 5 项 / 远程门诊/转诊/影像/病理/心电 | — | 0 | 平台标记"不需要对接" ✅ 与我方判断一致 |

**「接口联调结果发布」页复核（2026-07-04）**：该页实为**已上传测试数据的逐条反显浏览**
（可见外包商 2025-08-20 集中推送的造数记录，如远程会诊 50 条、测试医生"姜*伟/内科03"）。
年度统计图确认：**数据仅集中在 2025-08，2026 年全年无任何上传**——外包商自 2025-08 联调后再无动作，
与"从未上正式"结论一致。注意：平台会留存并反显我方推送的每条测试数据，
S5 联调时我方造数也应使用明显可辨识的测试标记（如患者名"测试患者"），避免与真实数据混淆。

**T2 至此关闭。** 必接接口范围最终确认为 10 个：9 个我方已全部实现，仅"远程会诊"待 T5 澄清。

## T3 · 测试环境密钥 + 权限/白名单 —— 🟡 密钥✅，权限/白名单实测定

1. ✅ 测试密钥已从"秘钥生成及管理"抄录并配置进 `backend/.env`（appSecret 为 32 位 hex，符合预期）。
2. **平台无自助"服务权限"勾选项、无 IP 白名单配置入口**（2026-07-03 确认）——说明这两项
   要么平台默认放开，要么由平台侧后台维护。**处理方式：直接跑 T4 实测**：
   - 返回 `40006`（无权限调用该服务）→ 联系平台对接人申请开通 9 个必接方法；
   - 返回 `40007`（IP 非法）→ 把生产服务器出口 IP 报给平台对接人加白，改在生产机上联调；
   - 未见上述错误 → 此项自然关闭。
3. 密钥保管：只进服务器/开发机 `.env`，不进 git、不进聊天记录。
4. 平台侧联调配套功能已确认存在（S5 要用）：**接口联调结果发布**（平台公布达标结论）、
   **测试接口双向反显**（核对我方推送的数据）、**接入证明下载**（达标凭证）。

**产出：** `.env` 已配置 ✅；40006/40007 是否出现的实测结论。

## T4 · 首次连通性验证（配置已就绪，执行一条命令即可）

脚本 [backend/scripts/tj_ping.py](../backend/scripts/tj_ping.py) 已改为**自动读取 `backend/.env`**（密钥已配好，无需改代码）。本地国密自检已通过（2026-07-03）。在**开发机或生产服务器**上执行：

```bash
cd backend && pip install gmssl httpx    # 生产容器内已含
python scripts/tj_ping.py
```

> 若返回 40007（IP 白名单），改到生产服务器容器里跑：
> `dc exec api python scripts/tj_ping.py`（需先把 TJ_* 配置追加到服务器 `.env`），
> 并把生产机出口 IP 提交平台加白（见 T3）。

脚本先跑**本地国密自检**（与官方 SDK 黄金向量逐字节对拍，不通过不会发网络请求），再向测试网关推 1 条演示药品（`useFlag=2` 取消态，不污染目录）。

结果判读：

| 返回 | 含义 | 处理 |
| :-- | :-- | :-- |
| `body.msgCode=200` | 全链路通 | ✅ T4 完成 |
| `code=40007` | IP 不在白名单 | 回 T3 补白名单 |
| `code=40004/40010` | 密钥/签名错 | 核对 appSecret 是否 32 hex；截图问平台 |
| `code=40006` | 服务权限未开 | 回 T3 申请方法权限 |
| `body.msgCode=-99` | 链路已通，字段缺失 | 视同通过（msg 会列缺失字段） |

**产出：** 请求/响应截图归档。

## 工作日咨询清单 —— ✅ 全部已答（2026-07-07 平台书面回签）

原 5 条待问项平台已全部书面答复（详见 [T5-口径确认函](specs/tianjin/T5-口径确认函-待发平台.md) 答复登记表）：

| 原问项 | 平台答复 | 结论 |
| :-- | :-- | :-- |
| ① 预审核恢复 | 运维大群 @毕老师（毕书源）做数据维护 | ✅ 有解，去运维群找毕老师 |
| ② 远程会诊保留是否违规 | 勾选须完成测试对接（已沿用）；生产无数据"应报未报"未担保 | ⚠️ 建议取消勾选消歧义 |
| ③ 正式密钥发放 | 上传互联网营业执照 → 审批部门发放；测试/正式两独立通道 | ✅ 路径明确 |
| ④ 达标沿用 | 可沿用，接口/unitID 未变 | ✅ 不必重刷 |
| ⑤ 口径三小问 | 无 uploadFile 接口；时间窗无要求；bussID 平台不去重（建议不重复） | ✅ 已消化（uploadFile 停用、01:30 不变、bussID 天然唯一） |

> **剩余需人工执行**：决定远程会诊去留；完成 T6 人员/药品补录；在生产执行预检、目录初始化和首批核验。

## T6 医生名册已取得（2026-07-04）

从"材料申报 》执业人员信息"取得外包商登记的完整名册（6 医师 + 4 药师），科目映射见
[医生诊疗科目对照-草稿.csv](../backend/data/tj_dicts/医生诊疗科目对照-草稿.csv)（内科03/妇产科05/中医科50，
与执业许可证科目吻合）。**身份证不入库文件，由运营在 admin 加密录入**：6 医师→医生端
（subject_code/dept_code/身份证），4 药师→账号管理（真实姓名/身份证）。录完医生、账号两个列表的
"监管备案"，再完成药品列表即通过 T6。
剩余：药品监管编码对照（发药房）。

## T5 补充 · 联系不到平台对接人时的自助路径（2026-07-04）

《互联网监管平台填报说明.doc》未留客服电话/QQ群，只有平台网址
`https://imssp.wsjk.tj.gov.cn/hosmit-hlwqd/home.html`。但**大部分问题无需找人**——
填报说明明确的自助路径 + 保守默认即可推进到开正式上报：

**⚠️ 原则：不要为问答案回头依赖外包商（第三方）。** 摆脱其卡脖子正是本项目目的；
外包商仅可作知识参考，绝不经手我方账号/密钥/正式申请。

**找平台的阶梯（从易到难）**：① 平台登录页"帮助"入口 → ② 填报说明指的自助菜单
（见下）→ ③ 2023 牌照申办时的卫健委经办渠道 → ④ 滨海新区/天津市卫健委信息化或医政科室
→ ⑤ 12345 转市卫健委要"互联网诊疗监管平台技术对接联系方式"。

**填报说明揭示的自助操作**：
- **业务范围调整（含远程会诊，对应 Q13）**：`申请子系统 》 材料申报 》 申报详情 》 材料变更`
  → 第 8 步"互联网名称信息"的"互联网诊疗业务"勾选里**取消"远程会诊"** → 走完 9~14 步提交
  → 卫健委重新审核通过后，"测试接口双向反显"中远程会诊"是否需要对接"变为否。
  （2026-07-04 实操验证入口存在，已进入变更流程；状态由"执照已上传"→"预审核"。）
  **注意**：① 除远程会诊外其他勾选不要动；② 这是全量重审，走后续步骤只核对不改内容。

  **⚠️ 远程会诊（2026-07-07 平台答复后更新）**：执业许可证只登记**诊疗科目**（内科/妇产科/中医科），
  不登记"远程会诊"等业务类型，证上无法判断——正常。平台 T5 答复：勾选须完成测试对接（已由历史达标沿用满足，
  不卡联调），但**正式环境长期无数据是否"应报未报"平台未担保**。**据此建议取消该勾选**彻底消除歧义
  （我院零业务、取消无损失）；操作需先经毕老师恢复预审核状态后再走材料变更。若维持保留则接受残余歧义、
  **仍无需开发 uploadMeetClinicIndicators**（生产无数据可报）。研发侧两条路都不产生新代码。

  **✅ 材料申报"预审核"恢复办法已明确（T5 答复，2026-07-07）**：误触"材料变更"后状态由
  "执照已上传"变为"预审核"（材料审批结果仍"通过"，内容未改）。平台答复：**进运维沟通群联系运维人员做
  数据维护——运维大群 @毕老师（毕书源，平台开发）** 改回"通过/执照已上传"。影响面：物理执业许可证
  与测试联调均不受影响；2026-07-22 截图已确认状态恢复、正式密钥已发放，此项关闭。
- **正式密钥/接入证明（对应 Q5/Q11）**：填报说明第四/五步——**"已获执业许可的历史机构直接进第五步"**
  （我院正属此列），上传执业许可后在 `秘钥生成及管理` 页取正式地址和正式密钥。
- **人员变更**：`监管服务管理 》 变更申请 》 执业人员变更`。
- **违规自查**：正式上报后异常（如科目未备案）在 `监管服务管理 》 机构违规数据查看` 自动标出。

**13 问按"是否卡上线"分类**：
- A 平台自助解决：Q5/Q11（正式密钥流程已明）、Q13（变更申请自助调）；
- B 不问也能推进（保守默认）：Q3（不确定就 `tj_smoke --count 50` 刷满）、Q8（我方已"先目录后处方"）、Q9/Q10（按规范默认，违规会被平台标出再调）；
- C 真需确认但不阻塞：Q12（重刷成本仅一条命令）、Q6（已实测不需白名单）。

> 结论：即便一个对接人都联系不到，靠平台自助 + 保守默认也能走到"开正式上报"。
> 确认函（下方）照发，但作为"锦上添花"，不作为推进前提。

---

## T5 · 口径确认清单（发平台对接人，要书面回复）

以下问题直接决定 S2/S3 的开发形态，逐条要到明确答复（规范修订记录中的平台侧作者可作为线索，或通过 T1 报备时建立的联系渠道）：

1. **业务归类**：我院业务为图文咨询 + 视频问诊（复诊）。确认：图文按 2.2.1 在线咨询（consultationType=1，onsultationAttribute=1 诊疗咨询）、视频按 2.2.2 在线复诊 + 2.2.10 电子病历上报，是否认可？
2. ~~2.2.9 预约挂号是否需要对接~~ → **已由"测试接口双向反显"页回答：需要**（我方补实现）。
3. ~~测试达标数据量~~ → **已回答：常规接口 50 条、目录/评价类 10 条**。仍需问：达标后切正式的确认流程（"接口联调结果发布"由谁触发）？
4. **上报时间窗**：正式环境每日推送有无时间要求？（我方计划每日 01:30 推前一日终态数据。）
5. **处方核销**：我院有药房自取（deliveryType=0）与物流配送（=1）两种；处方超效期未取药按 `deliveryType=4 自动失效` 上报是否正确？
6. **存量与幂等**：切换前由外包商上报的历史数据是否需要我方补报？同一 unitID 下 bussID 是否按唯一键幂等（重复上报会覆盖还是报错）？
7. **正式密钥重置**：重置后旧密钥是否立即失效？可否指定生效时间（配合切换窗口）？
8. **附件上传**：uploadFile 的单文件大小、类型限制；附件 id 有效期。
9. **依赖关系**：处方明细 drcode 是否必须先在药品目录备案成功后才能上报处方？（决定 S3 上报顺序。）
10. **评价时序**：业务数据 T+1 上报，当天产生的评价随次日批次一起传是否可以（规范说"须先完成业务数据推送否则拒收"）？
11. **正式环境密钥**：平台"机构业务详情"中正式 appKey/appSecret 目前为空——发放条件是什么（测试达标后自动发放/需申请）？发放时旧测试密钥是否继续可用？
12. **历史达标沿用**：测试环境的达标数据是原第三方系统 2025-08 所传。我院现更换为自建平台，
    该达标状态是否沿用、可否据此直接申请正式密钥？还是需要我方用自建系统重新联调达标？
13. **"需要对接"范围调整**：反显页中**远程会诊**被标记为需要对接（我院实际未开展远程会诊业务，
    疑为当年申报时误勾）——可否按实际业务范围调整为"不需要"？若不可，对接要求与达标量如何计？

**产出：** 问题-答复对照表，归档到 `docs/specs/tianjin/`。

## T6 · 字典对照表（医务/药房参与，与 T5 并行）—— 🟡 材料全备，只剩填表录入

基础材料已全部就绪（[backend/data/tj_dicts/](../backend/data/tj_dicts/)，含 README 填写说明）：
规范字典 3.1 诊疗科目 / 3.2 科室类别 / 3.3 医生职称 / 3.10 药品分类已转成 CSV（UTF-8-BOM，Excel 直接打开），
另备好人员/药品**填写模板**及安全批量导入程序。剩余为人工收集、复核和执行：

1. **医务**：使用[人员批量导入模板](templates/人员批量导入模板.xlsx)，每位在册医生一行
   （查 3.1/3.2 字典填编码）。填写副本包含身份证，只允许通过批准的加密渠道传输并放入服务器
   `chmod 600` 的 Git 工作区外目录，禁止进入仓库、聊天或普通邮件。医生本人须先登录医生端一次，
   再由 `scripts.import_personnel` 按授权手机号幂等补录。
   **⚠️ 科目范围硬约束（据执业许可证 2026-07-04）**：本院合法诊疗科目仅 **内科(03) / 妇产科(05) /
   中医科(50)** 三大类，医生 `subjectCode` 必须落在这三类之下（如 03.xx / 05.xx / 50.xx），
   不得出现三类之外的科目，否则正式上报会被平台判"科目未备案"。
2. **药师账号**：同一人员模板收集 4 名审方药师真实姓名、身份证和唯一用户名；导入程序幂等建档、
   加密身份证，并把每人不同的随机初始密码写入独立 `0600` 凭据文件。未补齐的账号无法审方通过。
3. **药房**：《对照模板-药品监管编码.csv》每个在售药品一行（drugClass 查 3.10；countrydrcode
   从说明书/医保编码库查，查不到先留空——非必输），交回后在 admin「药品字典 → 编辑」录入，
   保存即自动触发目录上报入队。**这份最费人工，尽早布置。**
4. ✅ ICD-10 导入源已就绪，部署时 `python -m scripts.import_icd10` 一键导入。

**验收：** 人员导入先 dry-run 为 0 ERROR；正式导入后 admin 医生、账号、药品三个列表的
“监管备案”全部为“已补录”，且生产运行 `python -m scripts.tj_preflight` 为 0 FAIL。

**辅助核对：** admin「监管合规 → CA人员进度」已提供医师/药师资料就绪和本人双录状态的只读汇总，
可在人员导入后快速定位缺项；该页面不查询放心签、不消耗核验次数，也不能替代上述监管备案列表及
`tj_preflight` 的正式验收。

## T7 · 机构信息核对与上线计划 —— ✅ 完成

1. ✅ **organID = `91120116MACJA9PX45`**（平台机构档案抄录，已入 `.env`）；建议与《医疗机构执业许可证》
   纸面再核一次。`ORGAN_NAME=天津逑贝互联网医院`。
2. **上线计划（原"切换窗口"作废）**：T2 已确认从未有人上报过，不存在切换与断报问题，
   简化为顺序推进：
   - 测试环境联调达标（S5，平台"接口联调结果发布"出结论）；
   - ✅ 正式环境密钥已发放（2026-07-22）；
   - 生产只读预检 0 FAIL → 首次真实药品目录入队 → 开启上报 → 每日 01:30 自动批次和不良事件签到；
   - 上线后第 2 天早上在 admin-web 监管面板核验首个自动批次全部 success。
   - 外包商侧唯一残留动作：T1 完成收权改密后，其登录凭据自然失效，无其他技术依赖。

**产出：** ✅ 机构信息与上线计划均已确定。生产执行归入 S5 第 3 步，不再作为 T7 未完成项。

---

## S0 完成判据（DoD）

- [ ] 我方掌握监管平台机构账号，密码已改，管理员为我方人员（🟡 已可登录，改密待办）
- [x] 存量上报现状有结论：**从未上报，从零接入**（2026-07-03 当时接口数据量统计全 0）
- [x] 测试环境 unitId/appKey/appSecret 到手（2026-07-03，已入 `backend/.env`）
- [x] 正式环境 appKey/appSecret 到手（2026-07-22；凭据不入文档、不入 git）
- [x] 方法权限/IP 白名单经 T4 实测确认无障碍（2026-07-04，未出现 40006/40007）
- [x] `tj_ping.py` 对测试网关调用返回 `msgCode=200`（2026-07-04，生产服务器容器内执行，输出已截图）
- [x] 口径问题取得书面答复（2026-07-07，13 问全部回签，无一推翻实现；答复登记表已归档）
- [ ] 人员/药品对照表交付并安全导入，医生身份证收集完成（🟡 模板、导入程序和 ICD-10 已就绪）
- [x] organID 确认（91120116MACJA9PX45，与平台档案一致；建议再与执业许可证核对一次）
- [x] 上线计划确定：无并行期顺序上线（原"切换窗口"因 T2 结论作废，见 T7）
