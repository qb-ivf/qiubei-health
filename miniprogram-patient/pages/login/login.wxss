.login { min-height: 100vh; display: flex; flex-direction: column; align-items: center; padding: 160rpx 64rpx 0; box-sizing: border-box; }
.login__logo { width: 160rpx; height: 160rpx; border-radius: 40rpx; background: var(--primary-fixed); display: flex; align-items: center; justify-content: center; margin-bottom: 40rpx; }
.login__btn { width: 100%; margin-top: 96rpx; }
.login__policy { margin-top: 32rpx; }
